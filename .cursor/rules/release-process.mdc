---
description: Branch and release workflow for all Meavo repos (never push to main)
alwaysApply: true
---

<!-- Managed centrally in meavo-agent-templates. Edit it there and re-run
     scripts/bootstrap-agent-docs.sh rather than changing this copy. -->

# Meavo Release Process

All Meavo repos use `main` (production) and `staging` (integration). Both are
protected: direct pushes are rejected, and a passing `Typecheck` check plus a
pull request are required.

## Never push to main or staging

```bash
# ❌ BAD — rejected by branch protection
git checkout main && git commit -m "..." && git push

# ✅ GOOD — branch off staging, then open a PR
git fetch origin
git checkout -b feat/my-change origin/staging
git commit -m "..."
git push -u origin feat/my-change
gh pr create --base staging
```

## The flow

1. Branch off `staging` using a `feat/`, `fix/`, or `chore/` prefix.
2. Open a PR into `staging`. CI runs and Vercel builds a preview deployment.
3. Merge with **squash** once green. Verify on the staging URL:
   `https://<project>-git-staging-meavo-gateway.vercel.app`
4. To release, open a PR from `staging` into `main` and merge with a
   **merge commit**. Merging to `main` deploys production.

Never squash `staging` into `main` — it rewrites the commits and leaves the two
branches permanently diverged.

## Deploying

Pushing to `main` is what deploys production, and it only happens by merging a
release PR. Never run `vercel --prod`.

## Shared database

Every app reads one Neon database. `DATABASE_URL` is scoped so that **every**
preview deployment — `staging` and every `feat/` branch alike — resolves the
`staging` Neon branch, and only `production` resolves production data. Preview and
production are separate variable records; never widen the production record back
to `preview`.

Cross-app URLs (`AUTH_URL`, `GATEWAY_URL`, `HOLS_SYNC_URL`) are still scoped to the
`staging` branch alone, so sign-in on a `feat/` preview can misbehave. Use the
staging URL for anything involving auth.

The `development` scope resolves the `dev` Neon branch where a repo defines one, so
a local dev server never writes production data either. Where no
`development`-scoped `DATABASE_URL` exists, supply your own `.env`.

## Blob storage

Preview deployments write to one shared `meavo-staging-blob` store; production keeps
its per-app stores. Since the `staging` database is a copy of production, it holds
rows referencing files in the production store that staging cannot read — so
downloading a pre-existing attachment on staging fails by design. Never widen a
production `*_READ_WRITE_TOKEN` back to `preview`; that is what used to let staging
write into the real bucket.

`@meavo/db` is pinned per app by git tag, so schema changes must stay backward
compatible — add columns rather than renaming or dropping them, since older apps
still read the same tables.

## Changing environment variables

Vercel resolves environment variables when a deployment is built, so editing a
variable does nothing to deployments that already exist. Redeploy afterwards, or
the change silently has no effect:

```bash
vercel redeploy https://<project>-git-staging-meavo-gateway.vercel.app \
  --scope meavo-gateway
```

## Auth on staging

`AUTH_SECRET`, `AUTH_GOOGLE_ID` and `AUTH_GOOGLE_SECRET` must be scoped to **both**
`production` and `preview`. Staging intentionally shares production's values here,
because the Google OAuth client already whitelists the staging redirect URIs. A
Production-only auth variable makes the preview build start Auth.js with no secret:
`/api/auth/*` returns 500 and `/` and `/login` redirect at each other forever.

Diagnose it directly rather than guessing from the browser:

```bash
curl -s https://<project>-git-staging-meavo-gateway.vercel.app/api/auth/providers
# healthy -> {"google":{...}}
# broken  -> {"message":"There was a problem with the server configuration..."}
```

Vercel Deployment Protection is off on every app project, so staging URLs are
publicly reachable and gated only by the app's own sign-in. Don't re-enable it
without telling the team — it makes every staging URL answer with Vercel's login
page, which masks real application errors behind what looks like an access problem.
